{
  "name": "@conecta-boi/etl-validation",
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "npm run build && node dist/test/test-all.js",
    "test:separator": "npm run build && node dist/test/test-separator-detection.js",
    "test:streaming": "npm run build && node dist/test/test-streaming.js",
    "test:mapping": "npm run build && node dist/test/test-header-mapping.js"
  },
  "dependencies": {
    "fast-csv": "^5.0.1",
    "papaparse": "^5.4.1",
    "zod": "^4.1.8"
  },
  "devDependencies": {
    "@types/papaparse": "^5.3.16",
    "typescript": "^5.8.3"
  }
}
